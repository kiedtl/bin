#!/bin/sh
#
# chmac - Change the MAC address temporarily.
# Must be run as root!
# (c) Kied Llaentenn (@kiedtl)

er=$(interface)

l set "${inter}" down
l set "${inter}" address "${1}"

config wlan0 down hw ether $1
l set "${inter}" up
wfi
