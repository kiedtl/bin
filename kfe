#!/bin/sh

# --- colors ---
image=$(printf "\033[7m")
label=$(printf "\033[m\033[1m")
reset=$(printf "\033[0m")

# --- ascii art ---

ascii="    ___
   (.· |
   (<> |
  / __  \
 ( /  \ /|
_/\ __)/_)
\/_____\/"

# --- information ---
. /etc/os-release;k=`uname -r`;while IFS=: read -r a b;do b=${b%kB};case $a in MemTotal)x=$((x+=b));t=$((t=b));;Shmem)x=$((x+=b));;MemFree|Buffers|Cached|SReclaimable)x=$((x-=b));;esac;done</proc/meminfo;x=$((x/=1024));t=$((t/=1024))

# --- display ---
echo "
${image}                  ${label}  USER    ${reset}${USER}@$(hostname)
${image}        ___       ${label}  OS      ${reset}${PRETTY_NAME:-?}
${image}       (.· |      ${label}  UPTIME  ${reset}`uptime`
${image}       (<> |      ${label}  EDITOR  ${reset}${EDITOR:-?}
${image}      / __  \     ${label}  SHELL   ${reset}${SHELL##*/}
${image}     ( /  \ /|    ${label}  MEMORY  ${reset}${x}MiB / ${t}MiB
${image}    _/\ __)/_)    ${label}  WM      ${reset}2bwm
${image}    \/_____\/     ${label}  PKGS    ${reset}`xbps-query -l | wc -l` (xbps)
${image}                  ${label}  TEMP    ${reset}`temp`
${image}                  ${label}  KERNEL  ${reset}$k
"
