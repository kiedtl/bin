#!/bin/sh
#
# bar - get various bar info for polybar
# (c) Kied Llaentenn

main() {
	mode=$(cat /tmp/dwm_info/current_layout)
	case $mode in
		"0") echo "[]=" ;;
		"1") echo "><>" ;;
		"2") echo "[M]" ;;
		"3") echo "(@)" ;;
		"4") echo "[\]" ;;
	esac
}

main "$@"
